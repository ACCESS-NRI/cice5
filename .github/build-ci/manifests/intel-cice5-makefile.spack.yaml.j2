spack:
  specs:
  - 'cice5@git.{{ ref }}=stable model=access-om2'
  packages:
    all:
      require:
      - '%{{ intel_compiler }}'
      - 'target={{ target }}'
  concretizer:
    unify: false
  view: false