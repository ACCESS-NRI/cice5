spack:
  specs:
  - 'cice5@git.{{ ref }}=stable {{ om2_1deg }}'
  - 'cice5@git.{{ ref }}=stable build_type=Debug {{ om2_1deg }}'
  packages:
    all:
      require:
      - '%{{ intel_compiler }}'
      - 'target={{ target }}'
  concretizer:
    unify: false
  view: false