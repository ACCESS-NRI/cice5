spack:
  specs:
  - 'cice5@git.{{ ref }}=stable {{ om2_1deg }}'
  packages:
    gcc:
      require:
      - '{{ gcc_compiler_ver }}'
    all:
      prefer:
      - '%access_gcc'
      - 'target={{ target }}'
  concretizer:
    unify: false
  view: false