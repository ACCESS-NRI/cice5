spack:
  specs:
  - 'cice5@git.{{ ref }}=stable model=access-om2'
  packages:
    gcc:
      require:
      - '{{ gcc_compiler_ver }}'
    all:
      prefer:
      - '%access_gcc'
      - 'target={{ target }}'
  concretizer:
    unify: false
  view: false