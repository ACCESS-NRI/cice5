spack:
  specs:
  - 'cice5@git.{{ ref }}=stable model=access-om2'
  packages:
    c:
      require:
      - {{ gcc_compiler }}
    cxx:
      require:
      - {{ gcc_compiler }}
    fortran:
      require:
      - {{ gcc_compiler }}
    all:
      prefer:
      - 'target={{ target }}'
  concretizer:
    unify: false
  view: false