
name: Test model build

on:
  workflow_dispatch:
  pull_request:
    branches:
      - "master"
      - "access-esm1.6"
  push:
    branches:
      - "master"
      - "access-esm1.6"
jobs:
  build:
    name: Build ${{ github.repository }} via spack
    uses: access-nri/build-ci/.github/workflows/model-1-build.yml@main
    with:
      rhs-branch: ${{ (github.base_ref == 'access-esm1.6' || github.ref_name == 'access-esm1.6') && 'access-esm1.6' }}
    permissions:
      packages: read
